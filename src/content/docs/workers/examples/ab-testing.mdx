---

summary: Cookie'lere dayalı olarak hangi yanıtın sunulacağını kontrol ederek A/B testi kurun. Bu sürüm, rastgele atamayı bypass ederek test ve kontrol için isteği origin'e geçirmeyi destekler.
tags:
  - JavaScript
  - TypeScript
  - Python
pcx_content_type: example
title: Aynı URL ile doğrudan erişimli A/B testi
sidebar:
  order: 1001
description: Cookie'lere dayalı olarak hangi yanıtın sunulacağını kontrol ederek A/B testi kurun. Bu sürüm, rastgele atamayı bypass ederek test ve kontrol için isteği origin'e geçirmeyi destekler.
---

import { TabItem, Tabs } from "~/components";

<Tabs syncKey="workersExamples"> <TabItem label="JavaScript" icon="seti:javascript">

```js
const NAME = "myExampleWorkersABTest";

export default {
	async fetch(req) {
		const url = new URL(req.url);

		// Kontrol ve test rotalarına doğrudan erişime izin vermek için Passthrough'u etkinleştirin.
		if (url.pathname.startsWith("/control") || url.pathname.startsWith("/test"))
			return fetch(req);

		// Bu istekte bulunanın hangi grupta olduğunu belirleyin.
		const cookie = req.headers.get("cookie");

		if (cookie && cookie.includes(`${NAME}=control`)) {
			url.pathname = "/control" + url.pathname;
		} else if (cookie && cookie.includes(`${NAME}=test`)) {
			url.pathname = "/test" + url.pathname;
		} else {
			// Cookie yoksa, bu yeni bir istemcidir. Bir grup seçin ve cookie'yi ayarlayın.
			const group = Math.random() < 0.5 ? "test" : "control"; // 50/50 bölünme
			if (group === "control") {
				url.pathname = "/control" + url.pathname;
			} else {
				url.pathname = "/test" + url.pathname;
			}
			// Değişmezlikten kaçınmak için yanıtı yeniden oluşturun
			let res = await fetch(url);
			res = new Response(res.body, res);
			// Kalıcı A/B oturumlarını etkinleştirmek için cookie'yi ayarlayın.
			res.headers.append("Set-Cookie", `${NAME}=${group}; path=/`);
			return res;
		}
		return fetch(url);
	},
};
```

</TabItem> <TabItem label="TypeScript" icon="seti:typescript">

```ts
const NAME = "myExampleWorkersABTest";

export default {
	async fetch(req): Promise<Response> {
		const url = new URL(req.url);
		// Kontrol ve test rotalarına doğrudan erişime izin vermek için Passthrough'u etkinleştirin.
		if (url.pathname.startsWith("/control") || url.pathname.startsWith("/test"))
			return fetch(req);
		// Bu istekte bulunanın hangi grupta olduğunu belirleyin.
		const cookie = req.headers.get("cookie");
		if (cookie && cookie.includes(`${NAME}=control`)) {
			url.pathname = "/control" + url.pathname;
		} else if (cookie && cookie.includes(`${NAME}=test`)) {
			url.pathname = "/test" + url.pathname;
		} else {
			// Cookie yoksa, bu yeni bir istemcidir. Bir grup seçin ve cookie'yi ayarlayın.
			const group = Math.random() < 0.5 ? "test" : "control"; // 50/50 bölünme
			if (group === "control") {
				url.pathname = "/control" + url.pathname;
			} else {
				url.pathname = "/test" + url.pathname;
			}
			// Değişmezlikten kaçınmak için yanıtı yeniden oluşturun
			let res = await fetch(url);
			res = new Response(res.body, res);
			// Kalıcı A/B oturumlarını etkinleştirmek için cookie'yi ayarlayın.
			res.headers.append("Set-Cookie", `${NAME}=${group}; path=/`);
			return res;
		}
		return fetch(url);
	},
} satisfies ExportedHandler;
```

</TabItem> <TabItem label="Python" icon="seti:python">

```py
import random
from urllib.parse import urlparse, urlunparse
from workers import Response, fetch, WorkerEntrypoint

NAME = "myExampleWorkersABTest"

class Default(WorkerEntrypoint):
	async def fetch(self, request):
		url = urlparse(request.url)
		# Yerel test sırasında aşağıdaki satırın yorumunu kaldırın
		# url = url._replace(netloc="example.com") if "localhost" in url.netloc else url

		# Kontrol ve test rotalarına doğrudan erişime izin vermek için Passthrough'u etkinleştirin.
		if url.path.startswith("/control") or url.path.startswith("/test"):
			return fetch(urlunparse(url))

		# Bu istekte bulunanın hangi grupta olduğunu belirleyin.
		cookie = request.headers.get("cookie")

		if cookie and f'{NAME}=control' in cookie:
			url = url._replace(path="/control" + url.path)
		elif cookie and f'{NAME}=test' in cookie:
			url = url._replace(path="/test" + url.path)
		else:
			# Cookie yoksa, bu yeni bir istemcidir. Bir grup seçin ve cookie'yi ayarlayın.
			group = "test" if random.random() < 0.5 else "control"
			if group == "control":
				url = url._replace(path="/control" + url.path)
			else:
				url = url._replace(path="/test" + url.path)

			# Değişmezlikten kaçınmak için yanıtı yeniden oluşturun
			res = await fetch(urlunparse(url))
			headers = dict(res.headers)
			headers["Set-Cookie"] = f'{NAME}={group}; path=/'
			return Response(res.body, headers=headers)

		return fetch(urlunparse(url))
```

</TabItem> <TabItem label="Hono" icon="seti:typescript">

```ts
import { Hono } from "hono";
import { getCookie, setCookie } from "hono/cookie";

const app = new Hono();

const NAME = "myExampleWorkersABTest";

// Kontrol ve test rotalarına doğrudan erişime izin vermek için passthrough'u etkinleştirin
app.all("/control/*", (c) => fetch(c.req.raw));
app.all("/test/*", (c) => fetch(c.req.raw));

// A/B testi mantığını işlemek için middleware
app.use("*", async (c) => {
	const url = new URL(c.req.url);

	// Bu istekte bulunanın hangi grupta olduğunu belirleyin
	const abTestCookie = getCookie(c, NAME);

	if (abTestCookie === "control") {
		// Kullanıcı kontrol grubunda
		url.pathname = "/control" + c.req.path;
	} else if (abTestCookie === "test") {
		// Kullanıcı test grubunda
		url.pathname = "/test" + c.req.path;
	} else {
		// Cookie yoksa, bu yeni bir istemcidir
		// Bir grup seçin ve cookie'yi ayarlayın (50/50 bölünme)
		const group = Math.random() < 0.5 ? "test" : "control";

		// Atanan gruba göre URL yolunu güncelleyin
		if (group === "control") {
			url.pathname = "/control" + c.req.path;
		} else {
			url.pathname = "/test" + c.req.path;
		}

		// Kalıcı A/B oturumlarını etkinleştirmek için cookie'yi ayarlayın
		setCookie(c, NAME, group, {
			path: "/",
		});
	}

	const res = await fetch(url);

	return c.body(res.body, res);
});

export default app;
```

</TabItem> </Tabs>
